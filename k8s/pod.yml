apiVersion: v1
kind: Pod
metadata:
  name: node-pod
  namespace: node-app

spec:
  containers:
    - name: node-container
      image: himanshutoshniwal7570/node-app
      ports:
        - containerPort: 8000
